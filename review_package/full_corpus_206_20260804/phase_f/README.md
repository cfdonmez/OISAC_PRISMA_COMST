# Phase F — S1–S7 Synthesis

Status: final analytical artifact set. Internal QA PASS; independent artifact-tool verification PASS 29/29.

Primary synthesis uses 8,203 claims: 3,020 evidence, 4,779 metric, and 404 trade-off claims. The inclusive 8,234 non-quarantined universe additionally includes 31 context-only metric claims and must not be reported as the primary evidence count. All 72 quarantined claims are excluded from primary numerical synthesis.

The exclusive modality, validation-maturity, artifact-availability, and 6G-relevance dimensions use the frozen Phase-E crosswalk. Integration, technology, and application are multi-label; `other` is fallback-only, with final counts of 0, 19, and 15 respectively.

The derivation and independent verifier accept host-supplied inputs through `OISAC_PHASE_D_WORKBOOK` and `OISAC_PHASE_E_CROSSWALK`; the verifier imports the workbook with artifact-tool. They enforce expected SHA-256 values rather than embedding workstation paths. Public-package QA metadata uses logical filenames only; counts, checks and source hashes are unchanged.

Provenance boundary: `independent_human_status = not_documented`.
