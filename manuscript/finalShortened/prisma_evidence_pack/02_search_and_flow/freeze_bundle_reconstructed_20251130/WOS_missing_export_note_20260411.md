# Missing Web of Science Export Note

No freeze-time `2025-11-30` Web of Science / Clarivate raw export is visible in the current repository snapshot.

Implication:

- The canonical `Web of Science = 250` identification claim is preserved only as a reconstructed aggregate count in `search_log.csv` and `formal_identification_reconstruction_20251130.csv`.
- This bundle therefore contains no row-level Web of Science export file corresponding to the declared freeze date.

Reviewer-safe wording:

- "The Web of Science contribution to the canonical freeze was retained as an aggregate reconstruction, but the original freeze-time raw export is not preserved in the current snapshot."
