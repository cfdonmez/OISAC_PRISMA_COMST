Image [_page_1_Picture_8.jpeg]: Diagram. This diagram illustrates a hybrid communication system involving an Unmanned Aerial Vehicle (UAV) and a terrestrial network. A UAV flies along an elliptical trajectory (indicated by dashed blue and purple lines with arrows), acting as a relay or part of the communication link. The UAV communicates with a ground station (represented by a cellular tower with concentric rings `R_in`, `R_o`, `R`, and an elliptical region `m` with width `Δ_m/2`). The UAV also establishes a Free-Space Optical (FSO) link with a distant FSO Gateway, which is depicted as another tall structure. This FSO link passes through a "Foggy FSO channel," indicating atmospheric impairments. Additionally, "Back-scattered light" is shown, originating from the UAV and interacting with the foggy channel, suggesting a potential backscatter communication or sensing mechanism. The diagram visualizes the spatial arrangement and key components of this integrated aerial-terrestrial-optical communication architecture.

Image [_page_4_Figure_1.jpeg]: Chart. This image displays two 3D charts, showing the optimal beamwidth (Λopt) and optimal bandwidth (Ωopt) as a function of the UAV's x and y coordinates.
*   **Chart (a) Beamwidth Λopt:** Plots optimal beamwidth (in degrees) against x and y coordinates. For `Θth = 10 dB` (blue circles), the beamwidth varies significantly, tracing a figure-eight path, with values ranging from approximately 40 to 170 degrees. For `Θth = -10 dB` (yellow squares), the beamwidth also follows a similar figure-eight path but generally exhibits higher values compared to `Θth = 10 dB` for most points along the trajectory, with values ranging from about 60 to 180 degrees.
*   **Chart (b) Bandwidth Ωopt:** Plots optimal bandwidth (in MHz) against x and y coordinates. For `Θth = 10 dB` (blue circles), the bandwidth varies, forming a figure-eight shape, with values ranging from approximately 40 to 250 MHz. For `Θth = -10 dB` (yellow squares), the bandwidth also follows a similar figure-eight path, but generally shows lower values for most points along the trajectory compared to `Θth = 10 dB`, with values ranging from approximately 30 to 200 MHz. In both charts, the x and y coordinates span from -500 m to 500 m, representing the spatial trajectory of the UAV.

Image [_page_4_Figure_11.jpeg]: Chart. This 2D chart illustrates the average edge user throughput and average UAV throughput (both in units of 10^8) as a function of visibility (V, in km).
*   **General Trend:** All four lines show an increasing trend in throughput as visibility increases from 0.5 km to 2 km.
*   **Impact of `ΦgE` (Ground-Edge component):** Lines including `Φm + ΦgE` (purple solid and magenta dashed) consistently show higher throughput than lines with only `Φm` (blue dash-dot and cyan dotted) for the same `Θth` value. This indicates that incorporating the ground-edge component `ΦgE` significantly enhances throughput. For instance, at V=2 km, `Φm + ΦgE, Θth = 10 dB` (purple) reaches approximately 10.5 x 10^8, while `Φm, Θth = 10 dB` (blue) reaches about 9.5 x 10^8.
*   **Impact of `Θth` (Threshold):** A `Θth` of 10 dB generally results in higher throughput compared to -10 dB.
    *   For `Φm + ΦgE`: The purple solid line (`Θth = 10 dB`) is above the magenta dashed line (`Θth = -10 dB`) across the entire visibility range. At V=2 km, the throughput for `Θth = 10 dB` is about 10.5 x 10^8, while for `Θth = -10 dB` it is about 8.5 x 10^8.
    *   For `Φm`: The blue dash-dot line (`Θth = 10 dB`) is above the cyan dotted line (`Θth = -10 dB`) across the entire visibility range. At V=2 km, the throughput for `Θth = 10 dB` is about 9.5 x 10^8, while for `Θth = -10 dB` it is about 7.5 x 10^8.
*   **Threshold Effect:** All throughput values start low at V=0.5 km (around 0.5 to 1 x 10^8) and show a steeper increase between V=0.5 km and V=1 km, before continuing to rise at a slightly gentler slope.

Image [_page_4_Figure_14.jpeg]: Chart. This 2D chart displays the average edge user throughput and average UAV throughput (both in units of 10^8) over time (in hours), exhibiting significant fluctuations.
*   **Fluctuating Trend:** All four lines show a highly dynamic, fluctuating pattern over the 91-hour period, with peaks reaching approximately 10-10.5 x 10^8 and troughs dropping to below 1 x 10^8. This suggests environmental factors or system operations that vary significantly hour-by-hour.
*   **Impact of `ΦgE`:** Similar to Figure 11, the lines including `Φm + ΦgE` (purple solid and magenta dashed) consistently show higher throughput values than their `Φm` counterparts (blue dash-dot and cyan dotted) for the same `Θth` at any given hour. This indicates the sustained benefit of the ground-edge component.
*   **Impact of `Θth`:** The `Θth = 10 dB` lines (purple solid and blue dash-dot) generally maintain higher throughput compared to the `Θth = -10 dB` lines (magenta dashed and cyan dotted) throughout the entire time series. The difference between `Θth = 10 dB` and `Θth = -10 dB` for `Φm + ΦgE` is visually larger than for `Φm` alone during peak throughput periods.
*   **Consistency of Relative Performance:** The relative performance hierarchy of the four configurations remains consistent despite the rapid fluctuations: `Φm + ΦgE, Θth = 10 dB` (highest) > `Φm + ΦgE, Θth = -10 dB` > `Φm, Θth = 10 dB` > `Φm, Θth = -10 dB` (lowest).

Image [_page_4_Figure_9.jpeg]: Chart. This image presents two 3D charts illustrating network throughput as a function of the UAV's x and y coordinates.
*   **Chart (a) Φm (End-to-end UAV network throughput):** Plots the end-to-end UAV network throughput (in units of 10^8) against x and y coordinates.
    *   For `Θth = 10 dB` (blue circles), the throughput traces a figure-eight path, with values generally ranging from approximately 0 to 11 x 10^8.
    *   For `Θth = -10 dB` (yellow squares), the throughput also follows a similar figure-eight path but consistently exhibits lower values compared to `Θth = 10 dB` for most points, ranging from approximately 0 to 9 x 10^8.
*   **Chart (b) Φm + ΦgE (Edge-users network throughput):** Plots the edge-users network throughput (in units of 10^8) against x and y coordinates.
    *   For `Θth = 10 dB` (blue circles), the throughput also traces a figure-eight path, with values generally ranging from approximately 0 to 12 x 10^8.
    *   For `Θth = -10 dB` (yellow squares), the throughput follows a similar figure-eight path but consistently shows lower values than `Θth = 10 dB` for most points, ranging from approximately 0 to 10 x 10^8.
*   **Comparison of (a) and (b):** Chart (b) (`Φm + ΦgE`) generally shows higher throughput values than Chart (a) (`Φm`) for both `Θth` conditions across the entire trajectory. For example, the peak throughput in (b) reaches around 12 x 10^8, while in (a) it reaches around 11 x 10^8. This indicates that the `ΦgE` component significantly contributes to the overall edge-users network throughput. The x and y coordinates for both charts span from -500 m to 500 m, describing the spatial performance over the UAV's trajectory.